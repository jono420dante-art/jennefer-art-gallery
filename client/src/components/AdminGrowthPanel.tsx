import { useEffect, useMemo, useRef, useState } from "react";
import { Activity, BarChart3, Download, ExternalLink, Globe2, MapPinned, MousePointerClick, RefreshCw, SearchCheck, ShieldCheck, Trash2, Users } from "lucide-react";
import { trpc } from "@/lib/trpc";
import { Button } from "@/components/ui/button";
import { Card } from "@/components/ui/card";
import { Tabs, TabsContent, TabsList, TabsTrigger } from "@/components/ui/tabs";
import { toast } from "sonner";

function MetricCard({ icon: Icon, label, value, detail, tone }: { icon: typeof Activity; label: string; value: number; detail: string; tone: string }) {
  return <Card className="min-w-0 rounded-lg border border-white/8 bg-[#101217] p-4"><div className="flex items-start justify-between gap-3"><div className="min-w-0"><p className="break-words text-[10px] font-bold uppercase tracking-[0.15em] text-slate-500">{label}</p><p className="mt-2 text-3xl font-black tracking-tight text-white">{value.toLocaleString()}</p><p className="mt-2 min-h-8 break-words text-xs leading-4 text-slate-400">{detail}</p></div><div className={`grid h-9 w-9 shrink-0 place-items-center rounded-lg ${tone}`}><Icon className="h-4 w-4 text-white" /></div></div></Card>;
}

function EmptyData({ text }: { text: string }) { return <p className="rounded-lg border border-dashed border-white/10 bg-white/[0.02] p-4 text-center text-sm leading-6 text-slate-400">{text}</p>; }

function SignalRows({ items, labelKey, valueKey, formatLabel }: { items: any[]; labelKey: string; valueKey: string; formatLabel?: (item: any) => string }) {
  const max = Math.max(...items.map((item) => Number(item[valueKey]) || 0), 0);
  return <div className="space-y-4">{items.map((item, index) => { const value = Number(item[valueKey]) || 0; return <div key={`${item[labelKey]}-${index}`}><div className="mb-1.5 flex items-center justify-between gap-3 text-xs"><span className="min-w-0 truncate capitalize text-slate-400">{formatLabel ? formatLabel(item) : item[labelKey]}</span><strong className="shrink-0 text-slate-100">{value}</strong></div><div className="h-1.5 overflow-hidden rounded-full bg-white/8"><div className="h-full rounded-full bg-gradient-to-r from-violet-500 to-blue-400" style={{ width: `${max ? (value / max) * 100 : 0}%` }} /></div></div>; })}</div>;
}

export function AdminGrowthPanel() {
  const [days, setDays] = useState(7);
  const [activeTab, setActiveTab] = useState(() => (typeof window !== "undefined" && new URLSearchParams(window.location.search).get("panel") === "seo" ? "seo" : "overview"));
  const [runtimeSeo, setRuntimeSeo] = useState({ homeTitle: "", homeDescription: "", robots: false, sitemap: false, structuredData: false });
  const homeAuditFrame = useRef<HTMLIFrameElement>(null);
  const artworkAuditFrame = useRef<HTMLIFrameElement>(null);
  const summaryQuery = trpc.analytics.summary.useQuery({ days }, { refetchInterval: 30_000 });
  const artworksQuery = trpc.artworks.list.useQuery();
  const utils = trpc.useUtils();
  const retentionMutation = trpc.analytics.clearExpired.useMutation({ onSuccess: () => { utils.analytics.summary.invalidate(); toast.success("Analytics records older than 90 days were removed."); }, onError: (error) => toast.error(error.message || "Could not apply the analytics retention rule.") });

  const downloadReport = async () => { try { const response = await fetch(`/api/admin/analytics-report?days=${days}`, { credentials: "include" }); if (!response.ok) throw new Error("Could not generate the PDF report."); const url = URL.createObjectURL(await response.blob()); const link = document.createElement("a"); link.href = url; link.download = `jennefer-ann-growth-report-${days}-days.pdf`; link.click(); URL.revokeObjectURL(url); toast.success("Your real-data growth report is downloading."); } catch (error) { toast.error(error instanceof Error ? error.message : "Could not generate the PDF report."); } };
  const openSeoDashboard = () => { setActiveTab("seo"); window.history.replaceState({}, "", `${window.location.pathname}?panel=seo#seo-dashboard`); window.setTimeout(() => document.getElementById("seo-dashboard")?.scrollIntoView({ behavior: "smooth", block: "start" }), 0); };

  useEffect(() => { let active = true; Promise.all([fetch("/robots.txt").then(async (response) => ({ ok: response.ok && (await response.text()).includes("User-agent:") })).catch(() => ({ ok: false })), fetch("/sitemap.xml").then(async (response) => ({ ok: response.ok && (await response.text()).includes("<urlset") })).catch(() => ({ ok: false }))]).then(([robots, sitemap]) => { if (active) setRuntimeSeo((current) => ({ ...current, robots: robots.ok, sitemap: sitemap.ok })); }); return () => { active = false; }; }, []);
  const inspectHomeFrame = () => { window.setTimeout(() => { const auditDocument = homeAuditFrame.current?.contentDocument; setRuntimeSeo((current) => ({ ...current, homeTitle: auditDocument?.title || "", homeDescription: auditDocument?.querySelector('meta[name="description"]')?.getAttribute("content") || "" })); }, 800); };
  const inspectArtworkFrame = () => { window.setTimeout(() => { const auditDocument = artworkAuditFrame.current?.contentDocument; setRuntimeSeo((current) => ({ ...current, structuredData: Boolean(auditDocument?.querySelector('script[type="application/ld+json"]')) })); }, 1_000); };
  const seoChecks = useMemo(() => { const title = runtimeSeo.homeTitle; const description = runtimeSeo.homeDescription; const hasAnalyticsTag = Boolean(document.querySelector('script[src*="googletagmanager.com/gtag/js"]')); return [{ label: "Homepage title", detail: `${title.length} characters — ${title || "Missing"}`, ok: title.length >= 30 && title.length <= 60 }, { label: "Meta description", detail: `${description.length} characters`, ok: description.length >= 50 && description.length <= 160 }, { label: "Google Analytics tag", detail: hasAnalyticsTag ? "Present in the public site head" : "Not detected", ok: hasAnalyticsTag }, { label: "robots.txt", detail: runtimeSeo.robots ? "Verified from /robots.txt" : "Checking published crawler directives…", ok: runtimeSeo.robots }, { label: "sitemap.xml", detail: runtimeSeo.sitemap ? "Verified from /sitemap.xml" : "Checking published sitemap…", ok: runtimeSeo.sitemap }, { label: "Artwork structured data", detail: artworksQuery.data?.[0] ? (runtimeSeo.structuredData ? "Verified on a rendered public artwork page" : "Checking a rendered public artwork page…") : "No artwork is available to audit yet", ok: runtimeSeo.structuredData }]; }, [artworksQuery.data, runtimeSeo]);
  const summary = summaryQuery.data;

  return <Card className="overflow-hidden rounded-xl border border-white/8 bg-[#15171c] text-slate-100"><iframe ref={homeAuditFrame} src="/" title="Public homepage SEO audit" className="hidden" onLoad={inspectHomeFrame} />{artworksQuery.data?.[0] && <iframe ref={artworkAuditFrame} src={`/artwork/${encodeURIComponent(artworksQuery.data[0].slug)}`} title="Public artwork structured-data audit" className="hidden" onLoad={inspectArtworkFrame} />}<div className="flex flex-col gap-5 border-b border-white/8 bg-[#101217] p-4 sm:p-5 lg:flex-row lg:items-center lg:justify-between"><div className="min-w-0"><div className="mb-2 flex items-center gap-2 text-violet-300"><BarChart3 className="h-4 w-4" /><span className="text-[10px] font-bold uppercase tracking-[0.17em]">Growth intelligence</span></div><h2 className="text-xl font-black tracking-tight text-white">Traffic, conversion and search readiness</h2><p className="mt-1 max-w-2xl break-words text-xs leading-5 text-slate-400">All traffic and interaction values below come from recorded public visits. No figures are estimated or generated.</p></div><div className="flex w-full flex-col gap-2 sm:w-auto sm:flex-row sm:flex-wrap"><select aria-label="Analytics reporting period" value={days} onChange={(event) => setDays(Number(event.target.value))} className="h-9 w-full rounded-md border border-white/12 bg-[#191c22] px-3 text-xs text-slate-100 sm:w-auto"><option value={1}>Today</option><option value={7}>Last 7 days</option><option value={30}>Last 30 days</option></select><Button variant="outline" size="sm" className="border-white/12 bg-white/[0.03] text-slate-100 hover:bg-white/[0.08] hover:text-white" onClick={() => summaryQuery.refetch()} disabled={summaryQuery.isFetching}><RefreshCw className={`mr-2 h-3.5 w-3.5 ${summaryQuery.isFetching ? "animate-spin" : ""}`} />Refresh</Button><Button size="sm" className="bg-violet-600 text-white hover:bg-violet-500" onClick={downloadReport}><Download className="mr-2 h-3.5 w-3.5" />PDF report</Button><Button variant="outline" size="sm" className="border-white/12 bg-white/[0.03] text-slate-100 hover:bg-white/[0.08] hover:text-white" onClick={openSeoDashboard}><SearchCheck className="mr-2 h-3.5 w-3.5" />SEO health</Button></div></div><div className="p-4 sm:p-5"><button id="seo-dashboard" type="button" onClick={openSeoDashboard} className="mb-5 flex w-full flex-col items-start justify-between gap-3 rounded-lg border border-violet-400/25 bg-violet-500/10 p-4 text-left transition hover:border-violet-400/50 hover:bg-violet-500/15 focus:outline-none focus:ring-2 focus:ring-violet-400 sm:flex-row sm:items-center"><span><span className="flex items-center gap-2 text-sm font-bold text-white"><SearchCheck className="h-4 w-4 text-violet-300" />SEO DASHBOARD</span><span className="mt-1 block text-xs leading-5 text-slate-400">Audit titles, descriptions, Google Analytics tagging, crawler directives, sitemap and artwork structured data.</span></span><span className="shrink-0 rounded-full bg-violet-500 px-3 py-1.5 text-[10px] font-bold tracking-wide text-white">Open SEO health</span></button><Tabs value={activeTab} onValueChange={setActiveTab}><TabsList className="mb-5 flex h-auto w-full justify-start overflow-x-auto rounded-lg bg-white/[0.04] p-1 [scrollbar-width:thin]"><TabsTrigger className="shrink-0 data-[state=active]:bg-[#262936] data-[state=active]:text-white" value="overview">Live overview</TabsTrigger><TabsTrigger className="shrink-0 data-[state=active]:bg-[#262936] data-[state=active]:text-white" value="traffic">Traffic & clicks</TabsTrigger><TabsTrigger className="shrink-0 data-[state=active]:bg-[#262936] data-[state=active]:text-white" value="seo">SEO health</TabsTrigger></TabsList><TabsContent value="overview" className="mt-0">{summaryQuery.isLoading ? <EmptyData text="Loading recorded visitor data…" /> : <><div className="grid grid-cols-1 gap-3 sm:grid-cols-2 xl:grid-cols-4"><MetricCard icon={Users} label="Unique sessions" value={summary?.uniqueSessions ?? 0} detail={`Real visitors in the selected ${days}-day period`} tone="bg-blue-600" /><MetricCard icon={Globe2} label="Page views" value={summary?.pageViews ?? 0} detail="Recorded public page loads" tone="bg-emerald-600" /><MetricCard icon={MousePointerClick} label="Conversion clicks" value={summary?.conversionClicks ?? 0} detail="Checkout, reserve, WhatsApp, commission and signup interactions" tone="bg-amber-600" /><MetricCard icon={Activity} label="Live now" value={summary?.activeVisitors ?? 0} detail="Sessions active in the last 30 minutes" tone="bg-violet-600" /></div><div className="mt-5 rounded-lg border border-emerald-400/15 bg-emerald-400/[0.06] p-3 text-xs leading-5 text-slate-400"><ShieldCheck className="mr-2 inline h-4 w-4 text-emerald-300" />Privacy-safe first-party tracking stores anonymous session IDs and conversion events. It does not store visitor email addresses or raw IP addresses.</div></>}</TabsContent><TabsContent value="traffic" className="mt-0"><div className="grid gap-3 lg:grid-cols-2"><Card className="rounded-lg border border-white/8 bg-[#101217] p-4"><h3 className="flex items-center gap-2 text-sm font-bold text-white"><Globe2 className="h-4 w-4 text-violet-300" />Traffic sources</h3><div className="mt-5">{summary?.trafficSources?.length ? <SignalRows items={summary.trafficSources} labelKey="source" valueKey="sessions" /> : <EmptyData text="Traffic sources will appear after real visitors arrive." />}</div></Card><Card className="rounded-lg border border-white/8 bg-[#101217] p-4"><h3 className="flex items-center gap-2 text-sm font-bold text-white"><ExternalLink className="h-4 w-4 text-violet-300" />Top referrers</h3><div className="mt-5">{summary?.referrers?.length ? <SignalRows items={summary.referrers} labelKey="referrerDomain" valueKey="sessions" /> : <EmptyData text="External links, social posts and campaign referrals will appear here." />}</div></Card><Card className="rounded-lg border border-white/8 bg-[#101217] p-4"><h3 className="flex items-center gap-2 text-sm font-bold text-white"><BarChart3 className="h-4 w-4 text-violet-300" />Most-viewed pages</h3><div className="mt-5">{summary?.topPages?.length ? <SignalRows items={summary.topPages} labelKey="pagePath" valueKey="views" /> : <EmptyData text="Top landing and artwork pages will show after public visits are recorded." />}</div></Card><Card className="rounded-lg border border-white/8 bg-[#101217] p-4"><h3 className="flex items-center gap-2 text-sm font-bold text-white"><MousePointerClick className="h-4 w-4 text-violet-300" />Highest-intent clicks</h3><div className="mt-5">{summary?.topClicks?.length ? <SignalRows items={summary.topClicks} labelKey="eventType" valueKey="clicks" formatLabel={(item) => `${item.eventType.replace("click_", "").replaceAll("_", " ")}${item.target ? ` · ${item.target}` : ""}`} /> : <EmptyData text="Buyer-intent interactions will appear once visitors use sales controls." />}</div></Card><Card className="rounded-lg border border-white/8 bg-[#101217] p-4 lg:col-span-2"><h3 className="flex items-center gap-2 text-sm font-bold text-white"><MapPinned className="h-4 w-4 text-violet-300" />Geographic view</h3><p className="mt-3 text-xs leading-5 text-slate-400">This privacy-safe first-party tracker intentionally does not store raw IP addresses or infer locations. For verified country and city reporting, use the connected Google Analytics property.</p><a href="https://analytics.google.com" target="_blank" rel="noopener noreferrer" className="mt-4 inline-flex items-center text-xs font-bold text-violet-300 hover:text-violet-200">Open Google Analytics geographic reports <ExternalLink className="ml-1 h-3.5 w-3.5" /></a></Card></div></TabsContent><TabsContent value="seo" className="mt-0"><div className="grid gap-3 lg:grid-cols-2">{seoChecks.map((check) => <Card key={check.label} className="rounded-lg border border-white/8 bg-[#101217] p-4"><div className="flex items-start justify-between gap-4"><div className="min-w-0"><h3 className="flex items-center gap-2 text-sm font-bold text-white"><SearchCheck className="h-4 w-4 text-violet-300" />{check.label}</h3><p className="mt-2 break-words text-xs leading-5 text-slate-400">{check.detail}</p></div><span className={`shrink-0 rounded-full px-2 py-1 text-[10px] font-bold ${check.ok ? "bg-emerald-400/15 text-emerald-300" : "bg-rose-400/15 text-rose-300"}`}>{check.ok ? "Ready" : "Needs attention"}</span></div></Card>)}</div><div className="mt-5 flex flex-wrap gap-2"><a href="/robots.txt" target="_blank" rel="noopener noreferrer"><Button variant="outline" size="sm" className="border-white/12 bg-white/[0.03] text-slate-100 hover:bg-white/[0.08] hover:text-white">Open robots.txt</Button></a><a href="/sitemap.xml" target="_blank" rel="noopener noreferrer"><Button variant="outline" size="sm" className="border-white/12 bg-white/[0.03] text-slate-100 hover:bg-white/[0.08] hover:text-white">Open sitemap.xml</Button></a><Button variant="outline" size="sm" className="border-white/12 bg-white/[0.03] text-slate-100 hover:bg-white/[0.08] hover:text-white" onClick={() => retentionMutation.mutate({ retentionDays: 90 })} disabled={retentionMutation.isPending}><Trash2 className="mr-2 h-3.5 w-3.5" />Keep 90 days of data</Button></div></TabsContent></Tabs></div></Card>;
}
